os: linux
dist: bionic

language:
    - c
    
before_install:
    - pip install --user cpp-coveralls
    
addons:
    sonarcloud:
        organization: "brovkinartem"
    token:
    secure: "52244f53753f11adacfc99b3a03abb3676713cc0"
    
script:
    - qmake 
    - make
    - ./tests/tests
    
after_success:
    - coveralls --root . -E ".*external.*" -E ".*tests.*"
    - build-wrapper-linux-x86-64 --out-dir bw-output make clean all
    - sonar-scanner -Dsonar.cfamily.build-wrapper-output=bw-output
    
notifications:
    email: false
